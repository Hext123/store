<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CommonDao">
	<select id="getID" resultType="String" parameterType="Map">
		exec [SP_Store_getID] #{tbName},#{keyName},#{qz}
	</select>
	
	<insert id="addT" parameterType="entity.T">
	insert into t values(#{name},#{js})
	</insert>
</mapper>